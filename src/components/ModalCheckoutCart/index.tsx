import { IModalCheckoutCart } from "../../interfaces"
import { Container } from "./style"

const ModalCheckoutCart = ({ setOpenModal, clearAllProducts }: IModalCheckoutCart) => {
  return(
    <Container>
      
    </Container>
  )
}

export { ModalCheckoutCart }
